# Renderer assets

Renderer resources are vector-only. Agent icons are inline SVG paths or standalone SVG files so the Desktop extension does not depend on raster images.

The supported external agents in the current Windows build are DeepSeek Harness and OpenCode. `codexhost-readme.svg` is a vector overview used by documentation and does not embed bitmap data.
